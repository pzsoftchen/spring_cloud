spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
      name: zipkin
#  cloud:
#    config:
#      uri: http://${config.host:192.168.1.140}:${config.port:8888}
server:
  port: 8082
